<%# TODO: This template overrides one in commonwealth-vlr-engine to
    prevent an attempt to render folder-related fields. The map potal
    application uses its own galleries instead of folders. %>
