load_defaults: 8.11.0
development:
  adapter: solr
  url: <%= ENV['SOLR_URL'] || "http://127.0.0.1:8983/solr/blacklight-core" %>
test: &test
  adapter: solr
  url: <%= ENV['SOLR_URL'] || "http://127.0.0.1:8983/solr/blacklight-core" %>
staging:
  adapter: solr
  url: <%= ENV.fetch('SOLR_URL') { Rails.application.credentials[:solr_url].to_s } %>
production:
  adapter: solr
  url: <%= ENV['SOLR_URL'] || Rails.application.credentials[:solr_url].to_s %>
